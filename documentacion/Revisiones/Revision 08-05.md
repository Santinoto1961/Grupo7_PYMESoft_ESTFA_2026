Daily:

* Bien

UML:

* Proveedor seria como un usuario? o se quieren guardar el dato de los proveedores? si es lo segundo deberia ser una funcion tipo registrarProveedor() que la deberia hacer el administrador creo
* Por como esta armado empleado deberia ser el el que alerte el stock no el propio producto
